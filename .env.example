# Ollama Configuration
# 먼저 별도 터미널에서 SSH 터널 연결 필요:
# ssh -L 11434:127.0.0.1:11434 server@<Mac_IP>
OLLAMA_URL=http://host.docker.internal:11434
MODEL_NAME=llama3:8b
# Cloudflare Tunnel 도메인을 입력하세요 (콤마로 여러 개 구분 가능)
# 예: ALLOWED_ORIGINS=https://your-tunnel.trycloudflare.com
# 개발 중에는 * 으로 설정 가능
ALLOWED_ORIGINS=*
